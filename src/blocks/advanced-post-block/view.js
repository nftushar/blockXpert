/**
 * Advanced Post Block - Frontend View
 * Handles frontend functionality for posts display
 */
document.addEventListener('DOMContentLoaded', function () {
  const blocks = document.querySelectorAll('.wp-block-blockxpert-advanced-post-block');
  
  blocks.forEach((block) => {
    // Add any frontend-specific interactions here
    // e.g., filtering, pagination, dynamic loading
  });
});
