/**
 * AI Product Recommendations - Frontend View
 * Handles frontend functionality for product recommendations
 */
document.addEventListener('DOMContentLoaded', function () {
  const blocks = document.querySelectorAll('.wp-block-blockxpert-ai-product-recommendations');
  
  blocks.forEach((block) => {
    // Add any frontend-specific interactions here
    // e.g., loading recommendations, filtering products
  });
});
